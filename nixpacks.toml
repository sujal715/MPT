[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:fullstack"]

[start]
cmd = "npm run start:fullstack"

[variables]
NODE_ENV = "production"
